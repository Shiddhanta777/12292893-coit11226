This week established the foundation for the unit, both technically and professionally.
Setting up GNS3 and GitHub helped me understand the importance of having a structured workflow for networking labs and documentation.
While configuring a static IP address using /etc/network/interfaces,
I realised how critical correct syntax is—small mistakes can prevent the network interface from working.
I also noticed that Linux hosts in GNS3 may have IP forwarding enabled by default, which differs from real-world configurations.
A key learning outcome was not just how to configure a node, but why documentation (using Markdown) is important for presenting technical work clearly.
